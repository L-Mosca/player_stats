plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
    id 'androidx.navigation.safeargs.kotlin'
    id 'kotlin-kapt'
    id 'com.google.dagger.hilt.android'
    id 'kotlin-parcelize'
    id 'kotlin-android'
    id 'com.google.gms.google-services'
}

android {
    signingConfigs {
        release {
            storeFile file(' ')
            storePassword ' '
            keyAlias ' '
            keyPassword ' '
        }
    }


    namespace 'br.com.lol_app'
    compileSdk 34

    defaultConfig {
        applicationId "br.com.lol_app"
        minSdk 24
        targetSdk 33
        versionCode 1
        versionName "0.2.0.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

        buildConfigField("String", "APP_NAME", "\"Player Stats/\"")
        buildConfigField("String", "API_SUMMONER_URL", "\"https://br1.api.riotgames.com\"")
        buildConfigField("String", "API_MATCH_URL", "\"https://americas.api.riotgames.com\"")
        buildConfigField("String", "API_KEY", "\" \"")
    }

    buildTypes {
        debug {
            debuggable true
            minifyEnabled false
        }
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }

    flavorDimensions 'environment'
    productFlavors {
        develop {
            applicationIdSuffix '.dev'
            resValue "string", "app_name", "[Beta] Player Stats"
            dimension 'environment'

            buildConfigField("String", "APP_NAME", "\"[Beta] Player Stats/\"")
            buildConfigField("String", "API_SUMMONER_URL", "\"https://br1.api.riotgames.com\"")
            buildConfigField("String", "API_MATCH_URL", "\"https://americas.api.riotgames.com\"")
            buildConfigField("String", "API_KEY", "\" \"")

        }
        prod {
            resValue "string", "app_name", "Player Stats"
            dimension 'environment'

            buildConfigField("String", "APP_NAME", "\"Player Stats/\"")
            buildConfigField("String", "API_SUMMONER_URL", "\"https://br1.api.riotgames.com\"")
            buildConfigField("String", "API_MATCH_URL", "\"https://americas.api.riotgames.com\"")
            buildConfigField("String", "API_KEY", "\" \"")
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    buildFeatures {
        buildConfig true
        viewBinding true
        dataBinding true
    }
    kotlinOptions {
        jvmTarget = '17'
    }
}

apply from: './dependencies.gradle'

